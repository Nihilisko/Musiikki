import ComingSoon from '../../components/ComingSoon';

export default function SettingsScreen() {
  return <ComingSoon items={['Note colors', 'Fretboard wood', 'Note labels (names / degrees)', 'Blue notes']} />;
}
